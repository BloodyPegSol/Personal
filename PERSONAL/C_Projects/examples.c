#include <stdio.h>
#include <sys/stat.h>
#include <fcntl.h>
#include "lib/tlpi_hdr.h"

#ifndef BUF_SIZE
#define BUF_SIZE 1024
#endif

int main(int argc, char *argv[])
{
	char buf[BUF_SIZE];
	ssize_t numRead;
	int fdIN, fdOUT;
	
		
	exit(EXIT_SUCCESS);
}
